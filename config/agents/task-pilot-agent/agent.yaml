id: task-pilot-agent
name: TaskPilot 默认 Agent
description: 默认兼容 Agent，保持现有计划执行流程。
version: "1"
enabled: true
mode: plans_executor
system_prompt_file: system_prompt.md
tools:
  - name: "*"
    description: 允许使用当前工具市场中可用的全部工具。
metadata:
  owner: core
  compatibility: legacy
