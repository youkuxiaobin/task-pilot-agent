cases:
  - id: default_react
    name: 默认 ReAct 链路
    input: 用 ReAct 模式完成一个简单任务，并总结最终结果。
    expected: 应走 react 模式，推理阶段、工具调用和最终答案都应可见。
    tags: [default, react, regression]
    metadata:
      checks:
        final_status: completed
        required_event_types: [task_created, task_running, agent_phase, result, task_completed]
